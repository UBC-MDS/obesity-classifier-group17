services:
  # run jupyter notebook inside jupyter 
  jupyter-notebook:
    image: zananpech/obesity-classifier-group17:c4e3ed9  
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan
    deploy:
      resources:
        limits:
          memory: 5G
    platform: linux/amd64
